What is the difference between a merge and a rebase?

merge combines changes while preserving the history while rebase rewrites the history by applying the changes on top of another branch
What is a remote repository?

a remote repositorary is where versions of a project are saved other than ones local machine
